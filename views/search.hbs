<h3>Your search of {{ search }}</h3>

{{#if hasContent}}
    {{#each imgs}}
        <div>
            <h5>{{ this.title }}</h5>
            <img src="{{ this.url }}" alt="">
        </div>
    {{/each}}
{{else}}
    <p>No search found</p>
{{/if}}    